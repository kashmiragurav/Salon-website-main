{
  "buildCommand": "npm install --prefix frontend && npm install --prefix admin-panel && npm run build --prefix frontend && npm run build --prefix admin-panel && cp -r admin-panel/dist frontend/dist/admin",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/admin/(.*)", "destination": "/admin/index.html" },
    { "source": "/(.*)",       "destination": "/index.html" }
  ]
}
