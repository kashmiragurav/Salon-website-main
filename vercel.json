{
  "framework": null,
  "installCommand": "npm install --prefix frontend && npm install --prefix admin-panel",
  "buildCommand": "npm run build --prefix frontend && npm run build --prefix admin-panel && rm -rf frontend/dist/admin && cp -r admin-panel/dist frontend/dist/admin",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    { "source": "/admin", "destination": "/admin/index.html" },
    { "source": "/admin/(.*)", "destination": "/admin/index.html" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
